// BSLLS:LineLength-off
// BSLLS:DuplicateStringLiteral-off
// BSLLS:MagicNumber-off

#Использовать asserts
#Использовать fluent
#Использовать fs
#Использовать tempfiles
#Использовать "helpers"
#Использовать "fixtures/benchmarks"

Перем _МенеджерВременныхФайлов; // МенеджерВременныхФайлов

#Область ОбработчикиСобытий

&Инициализация
Процедура ПередВсеми() Экспорт

	_МенеджерВременныхФайлов = Новый МенеджерВременныхФайлов();

КонецПроцедуры

&После
Процедура ПослеЗапускаТеста() Экспорт

	КаталогАртефактов = ".\BenchmarkArtifacts";

	ФС.УдалитьФайлы(КаталогАртефактов);

КонецПроцедуры

&Завершение
Процедура ПослеВсех() Экспорт

	_МенеджерВременныхФайлов.Удалить();	

КонецПроцедуры

#КонецОбласти

#Область Тесты

&Тест
Процедура ТестДолжен_ВернутьНеопределеноПриПодключенииНесуществующегоФайлаБенчмарка() Экспорт

	// Подготовка
	ПутьКФайлу = "./tests/fixtures/Классы/БенчмаркНесуществующий.os";

	// Действие
	Тип = Бенчмаркинг.ПодключитьБенчмарк(ПутьКФайлу);

	// Проверка
	Ожидаем.Что(Тип).ЭтоНеопределено();

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьНеопределеноПриПодключенииФайлаБезБенчмарков() Экспорт

	// Подготовка
	ПутьКФайлу = "./tests/fixtures/Классы/БенчмаркБезБенчмарков.os";

	// Действие
	Тип = Бенчмаркинг.ПодключитьБенчмарк(ПутьКФайлу);

	// Проверка
	Ожидаем.Что(Тип).ЭтоНеопределено();

КонецПроцедуры

&Тест
Процедура ТестДолжен_УбратьЦифрыВНачалеИмениТипаПриПодключенииФайла() Экспорт

	// Подготовка
	Каталог = _МенеджерВременныхФайлов.СоздатьКаталог();
	ПутьКФайлу = ОбъединитьПути(Каталог, "123ТестовыйБенчмарк.os");

	Код = "
	|&Бенчмарк
	|Процедура Бенчмарк() Экспорт
	|КонецПроцедуры";

	ЗаписьТекста = Новый ЗаписьТекста(ПутьКФайлу, КодировкаТекста.UTF8);
	ЗаписьТекста.Записать(Код);
	ЗаписьТекста.Закрыть();

	// Действие
	Тип = Бенчмаркинг.ПодключитьБенчмарк(ПутьКФайлу);

	// Проверка
	Ожидаем.Что(Строка(Тип)).Равно("ТестовыйБенчмарк");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПодключитьБенчмаркСУникальнымТипомПриКоллизииИмен() Экспорт

	// Подготовка
	Каталог = _МенеджерВременныхФайлов.СоздатьКаталог();
	ПутьКФайлу = ОбъединитьПути(Каталог, "ПустойБенчмарк.os");

	Код = "
	|&Бенчмарк
	|Процедура Бенчмарк() Экспорт
	|КонецПроцедуры";

	ЗаписьТекста = Новый ЗаписьТекста(ПутьКФайлу, КодировкаТекста.UTF8);
	ЗаписьТекста.Записать(Код);
	ЗаписьТекста.Закрыть();

	// Действие
	Тип = Бенчмаркинг.ПодключитьБенчмарк(ПутьКФайлу);
	
	// Проверка
	ИмяТипа = Строка(Тип);

	Ожидаем.Что(ИмяТипа).Не_().Равно("ПустойБенчмарк");
	Ожидаем.Что(Лев(ИмяТипа, СтрДлина("ПустойБенчмарк_"))).Равно("ПустойБенчмарк_");
	Ожидаем.Что(Тип).Не_().Равно(Тип("ПустойБенчмарк"));

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПередатьФактическийТипВСценарийИзолированногоЗапуска() Экспорт

	// Подготовка
	Каталог = _МенеджерВременныхФайлов.СоздатьКаталог();
	ПутьКФайлу = ОбъединитьПути(Каталог, "ПустойБенчмарк.os");

	Код = "
	|&Бенчмарк
	|Процедура Бенчмарк() Экспорт
	|КонецПроцедуры";

	ЗаписьТекста = Новый ЗаписьТекста(ПутьКФайлу, КодировкаТекста.UTF8);
	ЗаписьТекста.Записать(Код);
	ЗаписьТекста.Закрыть();

	ПодключенныйТип = Бенчмаркинг.ПодключитьБенчмарк(ПутьКФайлу);
	Конфигурация = Новый КонфигурацияБенчмарков(ПодключенныйТип);
	ТестированиеБенчмарков.НастроитьКонфигурациюПодТесты(Конфигурация);
	Конфигурация.УстановитьСтратегию(СтратегииЗапускаБенчмарка.ХолодныйЗапуск);

	// Действие
	Результат = Бенчмаркинг.Запустить(ПодключенныйТип, Конфигурация);

	// Проверка
	Дескриптор = Результат.ДескрипторыБенчмарков.ПолучитьПервый();
	Запуск = Результат.Запуски[0];

	Ожидаем.Что(Результат.ДескрипторыБенчмарков.Количество()).Равно(1);
	Ожидаем.Что(Результат.Запуски.Количество()).Равно(1);
	Ожидаем.Что(Результат.Отчет.Таблица.Количество()).Равно(1);
	Ожидаем.Что(Дескриптор.ТипКласса()).Равно(ПодключенныйТип);
	Ожидаем.Что(Запуск.Кейс.ДескрипторБенчмарка()).Равно(Дескриптор);
	Ожидаем.Что(Запуск.Кейс.ДескрипторБенчмарка().ТипКласса()).Равно(ПодключенныйТип);
	Ожидаем.Что(Запуск.Кейс.ДескрипторБенчмарка().ТипКласса()).Не_().Равно(Тип("ПустойБенчмарк"));
	Ожидаем.Что(Запуск.Замеры.Количество()).Равно(1);

КонецПроцедуры

#КонецОбласти
