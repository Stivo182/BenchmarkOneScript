// BSLLS:LineLength-off

#Использовать asserts
#Использовать fs
#Использовать tempfiles
#Использовать "helpers"
#Использовать "fixtures/benchmarks"

Перем _МенеджерВременныхФайлов; // МенеджерВременныхФайлов

&Инициализация
Процедура ПередВсеми() Экспорт
	_МенеджерВременныхФайлов = Новый МенеджерВременныхФайлов();
КонецПроцедуры

&Завершение
Процедура ПослеВсех() Экспорт
	_МенеджерВременныхФайлов.Удалить();
КонецПроцедуры

&Тест
Процедура ТестДолжен_ЗаписатьФайлСценарияИФайлНастроекЗапуска() Экспорт

	// Подготовка
	Сценарий = Новый СценарийЗапускаБенчмарка(
		Новый КоллекцияДескрипторовБенчмарков(Тип("ПустойБенчмарк")),
		ТестированиеБенчмарков.КонфигурацияПоУмолчанию()
	);
	ПутьКФайлуСценария = _МенеджерВременныхФайлов.НовоеИмяФайла("os");
	Кейс = КейсПустогоБенчмарка(17);

	// Действие
	Сценарий.Записать(ПутьКФайлуСценария, Кейс);

	// Проверка
	Ожидаем.Что(ФС.ФайлСуществует(ПутьКФайлуСценария)).Равно(Истина);
	Ожидаем.Что(ФС.ФайлСуществует(ПутьКФайлуНастроек(ПутьКФайлуСценария))).Равно(Истина);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ЗаписатьИдентификаторОдиночногоКейсаВНастройкиЗапуска() Экспорт

	// Подготовка
	Сценарий = Новый СценарийЗапускаБенчмарка(
		Новый КоллекцияДескрипторовБенчмарков(Тип("ПустойБенчмарк")),
		ТестированиеБенчмарков.КонфигурацияПоУмолчанию()
	);
	ПутьКФайлуСценария = _МенеджерВременныхФайлов.НовоеИмяФайла("os");
	ИдентификаторКейса = 23;

	// Действие
	Сценарий.Записать(ПутьКФайлуСценария, КейсПустогоБенчмарка(ИдентификаторКейса));
	НастройкиЗапуска = ПрочитатьНастройкиЗапуска(ПутьКФайлуНастроек(ПутьКФайлуСценария));

	// Проверка
	Ожидаем.Что(НастройкиЗапуска.ИдентификаторыКейсов).ИмеетДлину(1);
	Ожидаем.Что(НастройкиЗапуска.ИдентификаторыКейсов[0]).Равно(ИдентификаторКейса);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ЗаписатьИдентификаторыВсехКейсовИЧекпоинтВНастройкиЗапуска() Экспорт

	// Подготовка
	Сценарий = Новый СценарийЗапускаБенчмарка(
		Новый КоллекцияДескрипторовБенчмарков(Тип("ПустойБенчмарк")),
		ТестированиеБенчмарков.КонфигурацияПоУмолчанию()
	);
	ПутьКФайлуСценария = _МенеджерВременныхФайлов.НовоеИмяФайла("os");
	Кейсы = Новый Массив();
	Кейсы.Добавить(КейсПустогоБенчмарка(101));
	Кейсы.Добавить(КейсПустогоБенчмарка(102));

	Чекпоинт = Новый ЧекпоинтЗапускаБенчмарков();
	Чекпоинт.ИмяМетода = "Бенчмарк";
	Чекпоинт.ИдентификаторКейса = 102;
	Чекпоинт.Этап = "ПослеИтерации";
	Чекпоинт.НомерИтерации = 3;

	// Действие
	Сценарий.Записать(ПутьКФайлуСценария, Кейсы, Чекпоинт);
	НастройкиЗапуска = ПрочитатьНастройкиЗапуска(ПутьКФайлуНастроек(ПутьКФайлуСценария));

	// Проверка
	Ожидаем.Что(НастройкиЗапуска.ИдентификаторыКейсов).ИмеетДлину(2);
	Ожидаем.Что(НастройкиЗапуска.ИдентификаторыКейсов[0]).Равно(101);
	Ожидаем.Что(НастройкиЗапуска.ИдентификаторыКейсов[1]).Равно(102);
	Ожидаем.Что(НастройкиЗапуска.Чекпоинт.ИмяМетода).Равно("Бенчмарк");
	Ожидаем.Что(НастройкиЗапуска.Чекпоинт.ИдентификаторКейса).Равно(102);
	Ожидаем.Что(НастройкиЗапуска.Чекпоинт.Этап).Равно("ПослеИтерации");
	Ожидаем.Что(НастройкиЗапуска.Чекпоинт.НомерИтерации).Равно(3);

КонецПроцедуры

Функция КейсПустогоБенчмарка(Идентификатор)

	ДескрипторыБенчмарков = Новый КоллекцияДескрипторовБенчмарков(Тип("ПустойБенчмарк"));
	Возврат Новый КейсБенчмарка(Идентификатор, ДескрипторыБенчмарков.ПолучитьПервый());

КонецФункции

Функция ПутьКФайлуНастроек(ПутьКФайлуСценария)

	Файл = Новый Файл(ПутьКФайлуСценария);
	Возврат ОбъединитьПути(Файл.Путь, Файл.ИмяБезРасширения + ".settings.json");

КонецФункции

Функция ПрочитатьНастройкиЗапуска(ПутьКФайлу)

	Сериализатор = Новый СериализаторНастроекЗапускаБенчмарков();
	Возврат Сериализатор.ПрочитатьИзJson(ПутьКФайлу);

КонецФункции
