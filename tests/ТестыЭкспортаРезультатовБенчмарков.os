// BSLLS:LineLength-off
// BSLLS:MagicNumber-off

#Использовать asserts
#Использовать fs
#Использовать strings
#Использовать "helpers"
#Использовать "fixtures/benchmarks"

#Область ОбработчикиСобытий

&После
Процедура ПослеЗапускаТеста() Экспорт

	ФС.УдалитьФайлы(".\BenchmarkArtifacts");

КонецПроцедуры

#КонецОбласти

#Область Тесты

&Тест
&Параметры("Markdown", "md", "tests/fixtures/verified-report.md")
&Параметры("Json", "json", "tests/fixtures/verified-report.json")
&Параметры("Html", "html", "tests/fixtures/verified-report.html")
Процедура ТестДолжен_ЭкспортироватьОтчетВУказанныйФормат(ИмяЭкспортера, Расширение, ПутьКЭталону) Экспорт

	Если ТестированиеБенчмарков.ЭтоOneScript1() Тогда
		Возврат;
	КонецЕсли;

	// Подготовка
	Тип = Тип("БенчмаркиДляЭкспорта");
	ТекстЭталон = СтроковыеФункции.ПрочитатьФайл(ПутьКЭталону);
	Конфигурация = ПодготовитьКонфигурациюЭкспорта(Тип, ИмяЭкспортера);
	ПутьКФайлу = ОбъединитьПути(Конфигурация.КаталогАртефактов(), "БенчмаркиДляЭкспорта-report." + Расширение);

	// Действие
	Бенчмаркинг.Запустить(Тип, Конфигурация);

	// Проверка
	Ожидаем.Что(СтроковыеФункции.ПрочитатьФайл(ПутьКФайлу)).Равно(ТекстЭталон);

КонецПроцедуры

&Тест
Процедура ТестДолжен_НеСоздаватьАртефактыБезЭкспортеров() Экспорт

	// Подготовка
	Если ТестированиеБенчмарков.ЭтоOneScript1() Тогда
		Возврат;
	КонецЕсли;

	Тип = Тип("БенчмаркиДляЭкспорта");
	Конфигурация = ПодготовитьКонфигурациюЭкспорта(Тип);
	Конфигурация.УдалитьЭкспортеры();

	// Действие
	Бенчмаркинг.Запустить(Тип, Конфигурация);

	// Проверка
	Ожидаем.Что(НайтиФайлы(Конфигурация.КаталогАртефактов(), "*-report.*")).ИмеетДлину(0);

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПодготовитьКонфигурациюЭкспорта(Тип, ИмяЭкспортера = "")

	Конфигурация = Новый КонфигурацияБенчмарков(Тип);
	ТестированиеБенчмарков.НастроитьКонфигурациюПодТесты(Конфигурация);

	Конфигурация
		.ДобавитьОбработчикСобытия("ОбработчикиСобытийБенчмарков.УстановкаВремени", СобытияБенчмарков.ПослеКаждогоКейса)
		.ДобавитьОбработчикСобытия("ОбработчикиСобытийБенчмарков.УстановкаМокСредыОкружения", СобытияБенчмарков.ПослеВсех);

	Если ЗначениеЗаполнено(ИмяЭкспортера) Тогда
		Конфигурация.ДобавитьЭкспортер(ЭкспортерПоИмени(ИмяЭкспортера));
	КонецЕсли;

	Если ИмяЭкспортера = "Json" Тогда
		Конфигурация.УстановитьКоличествоИтераций(10);
	КонецЕсли;

	Возврат Конфигурация;

КонецФункции

Функция ЭкспортерПоИмени(ИмяЭкспортера)

	Если ИмяЭкспортера = "Markdown" Тогда
		Возврат ЭкспортерыРезультатовБенчмарков.Markdown;
	ИначеЕсли ИмяЭкспортера = "Json" Тогда
		Возврат ЭкспортерыРезультатовБенчмарков.Json;
	ИначеЕсли ИмяЭкспортера = "Html" Тогда
		Возврат ЭкспортерыРезультатовБенчмарков.Html;
	Иначе
		ВызватьИсключение СтрШаблон("Неизвестный экспортер <%1>", ИмяЭкспортера);
	КонецЕсли;

КонецФункции

#КонецОбласти
