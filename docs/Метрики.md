# Метрики

Метрики - это дополнительные статистические показатели, которые можно добавить в отчёт для детального анализа производительности.

## Min и Max

Минимальное и максимальное время выполнения за все итерации.

#### Аннотации

```bsl
&КолонкаМин
&КолонкаМакс
Процедура ПриСозданииОбъекта()
	// ...
КонецПроцедуры
```

#### API

```bsl
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.Мин);
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.Макс);
```

### Пример вывода

| Method   |     Mean |   StdErr |   StdDev |      Min |   Median |     Max |  Op/s |
|----------|---------:|---------:|---------:|---------:|---------:|--------:|------:|
| Бенчмарк | 208.6 ms | 88.62 ms | 198.2 ms | 10.19 ms | 10.48 ms | 1.001 s | 4.794 |

## Квартили

Разбивают упорядоченный ряд измерений на четыре равные части:

- **Q1** (нижний квартиль) — 25% измерений быстрее этого значения;
- **Q3** (верхний квартиль) — 75% измерений быстрее этого значения.

Вместе с медианой (Q2) квартили дают представление о форме распределения и позволяют обнаружить асимметрию без влияния выбросов.

#### Аннотации

```bsl
&КолонкаМедиана
&КолонкаНижнийКвартиль
&КолонкаВерхнийКвартиль
Процедура ПриСозданииОбъекта()
	// ...
КонецПроцедуры
```

#### API

```bsl
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.Медиана);
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.НижнийКвартиль);
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.ВерхнийКвартиль);
```

### Пример вывода

| Method   |     Mean |   StdErr |   StdDev |       Q1 |   Median |       Q3 |  Op/s |
|----------|---------:|---------:|---------:|---------:|---------:|---------:|------:|
| Бенчмарк | 26.55 ms | 1.849 ms | 7.160 ms | 7.625 us | 18.34 ms | 58.42 ms | 37.67 |

## Процентили

Показывают, сколько процентов измерений укладываются в заданное время. Например, P95 = 30 ms означает, что 95% запусков завершились быстрее 30 ms. Удобны для оценки хвостовых задержек.

По умолчанию добавляются колонки: P0, P25, P50, P67, P80, P85, P90, P95, P100.

#### Аннотации

```bsl
// Процентили по умолчанию
&КолонкаПроцентиль
Процедура ПриСозданииОбъекта()
	// ...
КонецПроцедуры

// Пользовательские процентили
&КолонкаПроцентиль(10, 40, 60, 80)
Процедура ПриСозданииОбъекта()
	// ...
КонецПроцедуры
```

#### API

```bsl
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.Процентиль);    // Процентили по умолчанию
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.Процентиль(10)); // 10-й процентиль
Конфигурация.ДобавитьКолонку(КолонкиОтчетаБенчмарков.Процентиль(40)); // 40-й процентиль
```

### Пример вывода

| Method   |     Mean |   StdErr |   StdDev |       P0 |      P25 |      P50 |      P67 |      P80 |      P85 |      P90 |      P95 |     P100 |  Op/s |
|----------|---------:|---------:|---------:|---------:|---------:|---------:|---------:|---------:|---------:|---------:|---------:|---------:|------:|
| Бенчмарк | 10.61 ms | 31.84 us | 123.3 us | 10.43 ms | 10.49 ms | 10.57 ms | 10.70 ms | 10.72 ms | 10.72 ms | 10.73 ms | 10.76 ms | 10.82 ms | 94.29 |